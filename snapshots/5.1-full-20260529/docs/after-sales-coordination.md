# 售后中心协调文档

## 当前交付范围

- 售后主单资源：`after-sales/services`
- 售后执行任务：`after-sales/tasks`
- 关联单据状态：`after-sales/linked-documents`
- 质量闭环：`after-sales/quality-actions`
- 第一阶段设置：售后原因、投诉问题、售后类型、处理方式

## 已确认规则

- 质量闭环不阻塞售后结单；售后结单后仍可显示“已关闭/质量追踪中”。
- “客户确认”调整为“结单确认”，由内部人员录入作为结单动作。
- 应收调整、发票红冲第一阶段先作为关联单据展示，并参与退款类结单条件。
- 配件出库单第一阶段并入出库单状态机。

## 状态机

- 入库单：待入库 -> 已入库 -> 已质检
- 出库单/配件出库单：待出库 -> 已出库 -> 已签收
- 付款单：待审核 -> 待付款 -> 已付款
- 服务派工：待派工 -> 服务中 -> 已完成
- 应收调整：待调整 -> 已调整
- 发票红冲：待红冲 -> 已红冲
- 质量闭环：待分析 -> CAPA执行中 -> 已验证 -> 已关闭

## 回填规则

- 退款退货：退货入库单已质检 + 退款付款单已付款 + 应收调整已调整 + 发票红冲已红冲 -> 待结单确认。
- 仅退款：退款付款单已付款 + 发票红冲已红冲 -> 待结单确认。
- 换货：退货入库单已质检 + 换货出库单已签收 -> 待结单确认。
- 仅退货：退货入库单已质检 + 应收调整已调整 -> 待结单确认。
- 维修/现场服务：服务派工单已完成 + 配件出库单已签收 -> 待结单确认。

## 页面入口

- 工作台：`/after-sales`
- 售后单列表：`/after-sales/services`
- 新增售后：`/after-sales/services?action=new`
- 售后详情：`/after-sales/services?id=as_001`
- 任务管理：`/after-sales/tasks`
- 任务详情：`/after-sales/tasks?id=task_doc_001`
- 质量闭环：`/after-sales/quality`
- 质量详情：`/after-sales/quality?id=qa_001`
- 售后设置：`/after-sales/settings`

## 验收镜子

- 静态验收镜子：`vue3/preview-after-sales.html`
- 本地 Vue 服务：`http://127.0.0.1:5177`

验收项：

- 工作台卡片可跳转到售后列表、任务列表、质量闭环和设置页。
- 售后列表字段、分类树、筛选、操作列符合主单入口要求。
- 新增售后可选择客户、来源单据、回填数量金额，并预览派生单据。
- 提交后自动生成退货入库、退款付款、换货出库、服务派工、配件出库、应收调整、发票红冲等关联单据。
- 售后详情的关联单据可推进状态，并反写主单汇总状态。
- 任务列表和详情从派生单据生成，推进任务会同步推进派生单据状态。
- 质量闭环可由售后详情升级生成，质量追踪不阻塞售后结单。
- 设置页第一阶段只维护四类字典，复杂编号、审批、策略、打印模板后续接公共设置组件。

## 交叉验证记录

- 第八步质量闭环：智能体复核未发现阻塞问题；已补充“验证关闭”Tab 的验证记录展示。
- 第九步售后设置：智能体复核未发现阻塞问题；确认只使用公共设置母版组件，未修改公共组件。
- 第九步返工记录：`售后原因`、`投诉问题`、`售后类型`、`处理方式` 是 `after-sales-list.jsx` 中由 `initialAction` 命中的预设字典列表，不是售后单业务列表，也不是可随意扩展的孤立设置页。此前错误在于没有先确认 JSX 的 action 分流规则，把 `/after-sales/services?action=售后原因` 等入口落到了售后单列表，导致页面出现“售后主题”等主单字段。
- 避免规则：后续遇到 `?action=`、`?setting=`、侧边飞出菜单、预设字典页时，必须先对照原 JSX 的入口分流、`showTree`、表格列、弹窗字段和示例数据；如果 JSX 定义与当前 Vue 实现不一致，先停下来确认，不得凭感觉改成通用列表或通用设置页。
- 避免规则：涉及用户明确点名的页面形态、母版差异、字段语义时，先用智能体做只读交叉验证，再实施；验证必须覆盖“入口是否命中正确页面”“是否显示错误业务字段”“是否修改了公共母版/其他模块”。不确定时先问用户。
- 2026-05-29 逻辑回正：售后飞窗不再统一走泛 `action`，状态类入口改走 `category`，质量闭环入口改走 `stage`；新增售后页已拆分“售后类型”和“处理方式”，投诉问题独立于售后原因；售后原因、投诉问题、售后类型、处理方式改为共享字典来源；可售后数量在页面提交和 mock API 层双重校验；“客户确认”可见文案已回正为“结单确认”。
- 2026-05-29 交叉验证补充：不单独伪造“结单确认任务”，结单确认入口回到售后单 `pendingCloseConfirm` 分类；处理方式到派生单据的映射由 API adapter 暴露单源，详情页读取同一映射判断回填完成。
- 非阻塞后续项：字典新增、编辑、删除当前为页面内存态，后续需要补 `saveAfterSalesDictionary` 或按字典项级别的后端契约。

## 构建记录

- 2026-05-29：`vue-tsc --noEmit` 通过。
- 2026-05-29：`vite build` 通过，仅保留 Vite chunk 体积警告，不阻塞售后中心验收。
- 2026-05-29：本轮售后逻辑回正后 `vite build` 通过；`vue-tsc --noEmit` 当前被研发中心 `RdResourcePage.vue` 既有缺失方法/变量错误阻塞，未修改研发模块。
