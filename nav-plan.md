# 采购中心导航重构计划

## 新侧栏菜单（sideItems）
| k | label |
|---|---|
| workbench | 工作台 |
| supplier | 供应商管理 |
| pr | 请购管理 |
| inquiry | 询价管理 |
| order | 采购管理 |
| pret | 采购退货 |
| exchange | 采购换货 |
| report | 采购报表 |

## 新 Flyouts 结构
```
supplier (供应商管理)
├── 供应商管理 → 添加供应商、供应商列表
└── 供应商设置 → 供应商分组设置、供应商自定义字段、供应商自定义编号、供应商等级设置、供应商审批设置、供应商策略设置

pr (请购管理)
├── 请购管理 → 添加请购、请购列表
└── 请购设置 → 请购自定义字段、请购自定义编号、请购审批设置、请购策略设置、设置采购打印模板

inquiry (询价管理)
├── 询价管理 → 添加询价、询价列表
└── 询价设置 → 询价自定义字段、询价自定义编号、询价审批设置、询价策略设置、设置采购打印模板

order (采购管理)
├── 采购管理 → 添加采购、采购列表
└── 采购设置 → 采购自定义字段、采购自定义编号、采购审批设置、采购策略设置、设置采购打印模板

pret (采购退货)
├── 退货管理 → 添加退货、退货列表
└── 退货设置 → 退货自定义字段、退货自定义编号、退货审批设置、退货策略设置、设置退货打印模板

exchange (采购换货)
├── 换货管理 → 添加换货、换货列表
└── 换货设置 → 换货自定义字段、换货自定义编号、换货审批设置、换货策略设置、设置换货打印模板

report (采购报表)
├── 采购报表 → 供应商对账表、按供应商查询
└── 采购统计 → 按产品统计、退货明细、换货明细
```

## 需要同步更新的
1. `MODULES` 新增模块定义
2. `PICK_MAP` 新增路由映射
3. `DEPT_CONFIG.pur` 整体替换
